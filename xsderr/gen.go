package xsderr

// catalog.go is regenerated from the local specs by tools/rulecat. It emits the
// Rule lookup set behind IsValidRule, covering every validation rule ID that
// appears in docs/specs/md. rulecat locates the module root itself, so this
// directive needs no path arguments. Run `go generate ./...` after editing the
// specs or rulecat.

//go:generate go tool rulecat
